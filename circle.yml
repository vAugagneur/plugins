machine:
  php:
    version: '5.6.5'

dependencies:
  pre:
    - git clone https://github.com/PrestaShop/PrestaShop-norm-validator ./vendor/PHP/CodeSniffer/Standards/Prestashop
  cache_directories:
    - './vendor'

test:
  override:
    - phpcs --standard=./vendor/PHP/CodeSniffer/Standards/Prestashop --colors --ignore=lib/,upgrade/ ./cashway-prestashop
